package design_pattern.adapter.adapter12

class MacOSWindowSystem {
    fun openMacWindow() {
        println("Opening a window on macOS...")
    }

    fun closeMacWindow() {
        println("Closing a window on macOS...")
    }
}